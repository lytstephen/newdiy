<% @page_title = 'CONFIRM YOUR ORDER' %>

<h4>Order Details</h4>

<div class="well">
	<section class="col-md-8 col-md-offset-2">
		<table class="table table-striped">
			<tr>
				<th>Coruse Title</th>
				<th>Type</th>
				<th>Quantity</th>
				<th>Unit Price</th>
				<th>Subtotal</th>
			</tr>
			<% @order.line_items.each do |line_item| %>
				<tr>
					<td><%= line_item.course.title %></td>
					<td><%= line_item.item_type %></td>
					<td><%= line_item.quantity %></td>
					<td>$<%= line_item.unit_price %></td>
					<td>$<%= line_item.subtotal %></td>
				</tr>
			<% end %>
			<tr>
				<td colspan="3">&nbsp;</td>
				<td>Order Total:</td>
				<td>$<%= @order.order_total %></td>
			</tr>
		</table>
	</section>
	<section class="row">&nbsp;</section>
</div>

<h4>Personal Information</h4>

<div class="well">

	<section class="col-md-4">
		<table class="table table-striped">
			<tr><th>First Name</th></tr>
			<tr><td><%= @user.first_name %></td></tr>
			<tr><th>Last Name</th></tr>
			<tr><td><%= @user.last_name %></td></tr>
			<tr><th>Email Address</th></tr>
			<tr><td><%= @user.email %></td></tr>
		</table>
	</section>

	<section class="col-md-4">
		<table class="table table-striped">
			<tr><th colspan="2">Shipping Address</th></tr>
			<tr><td colspan="2"><%= @user.shipping_add1 %></td></tr>
			<tr><td colspan="2"><%= @user.shipping_add2 %></td></tr>
			<tr><th colspan="2">Town / City</th></tr>
			<tr><td colspan="2"><%= @user.shipping_city %></td></tr>
			<tr><th>State</th><th>ZIP<th></tr>
			<tr><td><%= @user.shipping_state %></td><td><%= @user.shipping_zip %></tr>
			<tr><th colspan="2">Country</th></tr>
			<tr><td colspan="2"><%= @user.shipping_country %></td></tr>
		</table>
	</section>

	<section class="col-md-4">
		<table class="table table-striped">
			<tr><th colspan="2">Billing Address</th></tr>
			<tr><td colspan="2"><%= @user.billing_add1 %></td></tr>
			<tr><td colspan="2"><%= @user.billing_add2 %></td></tr>
			<tr><th colspan="2">Town / City</th></tr>
			<tr><td colspan="2"><%= @user.billing_city %></td></tr>
			<tr><th>State</th><th>ZIP<th></tr>
			<tr><td><%= @user.billing_state %></td><td><%= @user.billing_zip %></tr>
			<tr><th colspan="2">Country</th></tr>
			<tr><td colspan="2"><%= @user.billing_country %></td></tr>
		</table>
	</section>

	<div class="row">&nbsp;</div>

</div>

<h4>Payment Method</h4>

<div class="well ">

	<%= form_for @order, class: 'form-group' do |f| %>
		
		<section class="col-md-12">
			<div class="radio">
				<%= f.radio_button :pmt_method, 'cheque' %>
				<%= label :pmt_method_cheque, 'Cheque' %>
			</div>
			<div class="radio">
				<%= f.radio_button :pmt_method, 'transfer' %>
				<%= label :pmt_method_transfer, 'Bank Transfer' %>
			</div>
			<div class="radio">
				<%= f.radio_button :pmt_method, 'credit' %>
				<%= label :pmt_method_credit, 'Credit Card' %>
			</div>
		</section>

		<div class="row">&nbsp;</div>
		
</div>

<%= f.submit 'Proceed to Payment', class: 'btn btn-primary btn-lg', style: 'float:right' %>

<% end %>

















